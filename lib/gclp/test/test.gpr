with "../gclp.gpr";

project Test is

   for Object_Dir use "bin";

   package Compiler is
      for Default_Switches ("ada") use ("-gnat05", "-gnata", "-g");
   end Compiler;

   for Main use ("test.adb");

end Test;

